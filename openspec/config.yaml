# openspec/config.yaml — SDD context for tbhbuildshare
# Auto-generated by sdd-init on 2026-05-31
project: tbhbuildshare
description: "Static SPA for Task Bar Hero (TBH) build planning and sharing — hero selector,
  skill tree builder, gear loadout, pet selection, stat display, build sharing via URL
  hash and localStorage persistence."

context: >
  Pure frontend project. 5 core files (~2600 lines), zero dependencies, no build step.
  Served via Caddy as static files. Data layer is a hardcoded game database (data.js)
  with 6 heroes, 36 active skills, 8 pets, and gear rarity scale. Persistence is
  localStorage-only — no API, no backend. No tests, no CI, no package.json.

stack:
  language: "JavaScript (ES6)"
  runtime: "Browser"
  framework: "None (vanilla JS)"
  bundler: "None"
  server: "Caddy (static file server)"
  total_dependencies: 0

files:
  count: 7
  main:
    - index.html    # Build planner — hero, skill tree, gear, pets, stats, export/import
    - browse.html   # Build gallery — search, filter, sort, import/delete
    - app.js        # Planner logic — skill tree inc/dec/maxout/reset, equipment, sharing
    - data.js       # Game database — 6 heroes, 36 skills, 8 pets, gear types/rarities
    - styles.css    # Dark theme, responsive, game-oriented UI (~800 lines)
  support:
    - setup-caddy.sh
    - fix-caddy.sh

architecture: >
  Static SPA. Class-based MVC via TBHBuildPlanner. Data flows: data.js → app.js →
  DOM render. State persistence via localStorage. Build sharing via base64-encoded
  JSON in URL hash (#build=...). No routing library, no virtual DOM, no framework.

strict_tdd: false
strict_tdd_reason: >
  No test runner, no test files, no CI, no package.json exist in the project.
  Strict TDD cannot be enforced without a test infrastructure.

testing:
  runner: null
  frameworks: []
  layers:
    unit:        { available: false, tool: null }
    integration: { available: false, tool: null }
    e2e:         { available: false, tool: null }
  coverage:      { available: false, command: null }
  quality:
    linter:         { available: false, command: null }
    type_checker:   { available: false, command: null }
    formatter:      { available: false, command: null }

phases:
  proposal: { enabled: true }
  spec:     { enabled: true }
  design:   { enabled: true }
  tasks:    { enabled: true }
  apply:    { enabled: true }
  verify:
    enabled: true
    note: "Manual verification only — no test infrastructure available. All verification is browser-based manual testing."
  archive: { enabled: true }
